# same deployment products being exposed using ClusterIP instead of LoadBalancer
# This would allow private cluster 
apiVersion: v1
kind: Service
metadata:
  name: products-service
spec:
  type: ClusterIP
  selector:
    name: products
    app: micro
  ports:
  - name: grpc
    port: 80
    targetPort: 8081